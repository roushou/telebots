# See bots/imagine/README.md for where each key comes from.
TELEBOTS_TELEGRAM_API_KEY=
CLOUDFLARE_API_TOKEN=
CLOUDFLARE_ACCOUNT_ID=
# SQLite database path; /data/imagine.db in the container (volume-mounted).
IMAGINE_DB_PATH=/data/imagine.db
# Optional: metrics port the monitor polls (default 9102).
# TELEBOTS_METRICS_PORT=9102
