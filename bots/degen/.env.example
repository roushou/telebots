# Production values for the degen bot's container.
# scripts/up.sh creates this file from the example on first run; fill it in
# on the VPS. See bots/degen/README.md for where each key comes from.
TELEBOTS_TELEGRAM_API_KEY=
COINMARKETCAP_API_KEY=
# Optional: metrics port the monitor polls (default 9101).
# TELEBOTS_METRICS_PORT=9101
