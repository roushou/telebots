# Production values for the degen bot's container.
# deploy.sh creates this file from the example on first run; fill it in on
# the VPS. See bots/degen/README.md for where each key comes from.
TELEBOTS_TELEGRAM_API_KEY=
COINMARKETCAP_API_KEY=
